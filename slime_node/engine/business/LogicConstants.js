const CONDITION = {
IF: "if"
, ELSE: "else"
, SWITCH: "switch"
, CASE: "case"
};

const LOOP = {
FOR: "for"
WHILE: "while"
};

const BRANCHING = {
BREAK: "break"
, CONTINUE: "continue"
, RETURN: "return"
};

const DEFAULT = {
DEFAULT: "default"
, ZERO: "zero"
};

module.exports = {
CONDITION, LOOP, BRANCHING, DEFAULT
};